# @mysimcha/landing

Multi-brand marketing websites.

**Status:** minimal bootable App Router shell (no business features).

## Local

```bash
pnpm --filter=@mysimcha/landing dev
```

- Port: `3002`
- Health: `GET /api/health`
