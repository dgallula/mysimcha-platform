# @mysimcha/admin

Platform administration dashboard.

**Status:** minimal bootable App Router shell (no business features).

## Local

```bash
pnpm --filter=@mysimcha/admin dev
```

- Port: `3001`
- Health: `GET /api/health`
