# @mysimcha/web

Main customer application and guest invitation runtime.

**Status:** minimal bootable App Router shell (no business features).

## Local

```bash
pnpm --filter=@mysimcha/web dev
```

- Port: `3000`
- Health: `GET /api/health`
