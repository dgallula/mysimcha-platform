# @mysimcha/docs-site

Technical documentation site shell.

**Status:** minimal bootable App Router shell. Canonical markdown remains in `/docs`.

## Local

```bash
pnpm --filter=@mysimcha/docs-site dev
```

- Port: `3003`
- Health: `GET /api/health`
